import { notFound } from "next/navigation";

// Placeholder: the certificate page isn't built yet. Keeping the route valid
// (a bare comment file isn't a module and breaks `next build`) and 404-ing
// until it is.
export default function CertificateRoute() {
  notFound();
}
